function data = data_to_pov(data)
% DATA_TO_POV - takes an a Nx(3+) array.  Treats the frist three columns as (x,y,z) and outputs a text file suitable to be shoved it to pov-ray and make a pretty picture
%   
end
